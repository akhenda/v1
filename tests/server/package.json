{
  "name": "@v1/example-test-server",
  "type": "module",
  "scripts": {
    "clean": "rm -rf .turbo node_modules coverage",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run"
  },
  "dependencies": {
    "fastify": "^4.13.0"
  },
  "devDependencies": {
    "@types/supertest": "^6.0.2",
    "@vitest/coverage-v8": "^2.1.8",
    "@vitest/ui": "2.1.8",
    "supertest": "^7.0.0",
    "vitest": "2.1.8"
  }
}
